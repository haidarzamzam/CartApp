class CartModel {
  final String id;
  final String title;
  final int quantity;
  final double price;
  final String variant;

  CartModel({this.id, this.title, this.price, this.quantity, this.variant});
}
